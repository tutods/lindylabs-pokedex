import { BadgeBackground, IconContainer } from './styles';

const PokedexBadge = () => {
	return (
		<BadgeBackground>
			<IconContainer>🎒</IconContainer>
			pokedex
		</BadgeBackground>
	);
};

export { PokedexBadge };
